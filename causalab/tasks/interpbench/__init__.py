"""InterpBench / tracr task package (case-agnostic).

Loaded by ``causalab.tasks.loader.load_task("interpbench", {"case": ...})``.
The case is selected from ``causal_models.CASES`` via ``task_cfg["case"]``.
"""
